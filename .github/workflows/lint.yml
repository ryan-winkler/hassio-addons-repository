name: Lint

on:
  push:
    branches: [main]
    paths-ignore:
      - "**/config.*"
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  actions: read

jobs:
  lint-changes:
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set diff range
        run: |
          NULL_SHA="0000000000000000000000000000000000000000"
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "DIFF_RANGE=${{ github.event.pull_request.base.sha }}...${{ github.sha }}" >> $GITHUB_ENV
          elif [ "${{ github.event.before }}" = "$NULL_SHA" ]; then
            # First push to a new branch — diff against empty tree
            echo "DIFF_RANGE=$(git hash-object -t tree /dev/null)...${{ github.sha }}" >> $GITHUB_ENV
          else
            echo "DIFF_RANGE=${{ github.event.before }}...${{ github.sha }}" >> $GITHUB_ENV
          fi

      - name: Fix non-printable Unicode spaces in changed text files
        run: |
          set -euo pipefail
          CHANGED_FILES=$(git diff --name-only "$DIFF_RANGE")
          UNICODE_SPACES_REGEX=$'[\u00A0\u2002\u2003\u2007\u2008\u2009\u202F\u205F\u3000\u200B]'
          for file in $CHANGED_FILES; do
            if [ -f "$file" ]; then
              MIME_TYPE=$(file --mime-type -b "$file")
              if [[ "$MIME_TYPE" == text/* ]]; then
                perl -CSD -pe "s/$UNICODE_SPACES_REGEX/ /g" "$file" > "$file.tmp" && mv "$file.tmp" "$file"
              fi
            fi
          done

      - name: Run Super Linter
        uses: super-linter/super-linter/slim@main
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_ALL_CODEBASE: false
          VALIDATE_CHECKOV: false
          VALIDATE_PYTHON_PYLINT: false
          VALIDATE_JSCPD: false
          VALIDATE_NATURAL_LANGUAGE: false
          FILTER_REGEX_EXCLUDE: .github/workflows/.*
          FIX_SHELL_SHFMT: true
          FIX_ENV: false
          FIX_HTML_PRETTIER: false
          FIX_YAML_PRETTIER: false
          FIX_JSON: false
          FIX_MARKDOWN: false

      - name: Restore executable permissions
        run: |
          find . -type f \( -name "*.sh" -o -name "run" \) -exec chmod +x {} \;

      - name: Remove Super-Linter output
        run: sudo rm -rf super-linter-output/

  lint-autofix:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.TAG_PUSH_TOKEN }}

      # Runs before the linters so shfmt and prettier format files that
      # already have LF endings, rather than having their output rewritten.
      - name: Find and fix CRLF endings
        run: |
          find . -type f \
            -not -path './.git/*' \
            -not -name '*.png' \
            -not -name '*.jpg' \
            -not -name '*.bin' \
            -exec grep -l $'\r' {} \; | while read file; do
            dos2unix -k "$file"
            echo "Fixed: $file"
          done

      - name: Fix non-printable Unicode spaces in all text files
        run: |
          set -euo pipefail
          UNICODE_SPACES_REGEX=$'[\u00A0\u2002\u2003\u2007\u2008\u2009\u202F\u205F\u3000\u200B]'
          find . -type f ! -path "./.git/*" | while read -r file; do
            MIME_TYPE=$(file --mime-type -b "$file")
            if [[ "$MIME_TYPE" == text/* ]]; then
              perl -CSD -pe "s/$UNICODE_SPACES_REGEX/ /g" "$file" > "$file.tmp" && mv "$file.tmp" "$file"
            fi
          done

      - name: Run Super Linter
        uses: super-linter/super-linter/slim@main
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_ALL_CODEBASE: true
          VALIDATE_CHECKOV: false
          VALIDATE_PYTHON_PYLINT: false
          VALIDATE_JSCPD: false
          VALIDATE_NATURAL_LANGUAGE: false
          FILTER_REGEX_EXCLUDE: .github/workflows/.*
          FIX_SHELL_SHFMT: true
          FIX_ENV: true
          FIX_HTML_PRETTIER: true
          FIX_YAML_PRETTIER: true
          FIX_JSON: false
          FIX_MARKDOWN: true

      - name: Use 4 spaces in shell scripts
        run: |
          curl -sSLo /usr/local/bin/shfmt https://github.com/mvdan/sh/releases/download/v3.12.0/shfmt_v3.12.0_linux_amd64
          chmod +x /usr/local/bin/shfmt
          find . -type f \( -name "*.sh" -o -name "run" \) -exec shfmt -w -i 4 -ci -bn -sr {} +

      - name: Restore executable permissions
        run: |
          find . -type f \( -name "*.sh" -o -name "run" \) -exec chmod +x {} \;

      - name: Remove Super-Linter output
        run: sudo rm -rf super-linter-output/

      - name: Open lint autofix PR if changes
        env:
          GH_TOKEN: ${{ secrets.TAG_PUSH_TOKEN }}
        run: |
          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes detected"
            exit 0
          fi

          BRANCH="bot/lint-autofix"

          # main is protected (PR required), so this can never push
          # directly. If an autofix PR is already open, stack this run's
          # fixes on top of it instead of opening a second one.
          EXISTING_PR=$(gh pr list --head "$BRANCH" --state open --json number --jq '.[0].number' || true)

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          if [ -n "$EXISTING_PR" ]; then
            # The linters already ran and can't be cheaply re-run, so carry
            # the fixed working tree over to the existing PR branch.
            git stash push --include-untracked
            git fetch origin "$BRANCH"
            git checkout -B "$BRANCH" "origin/$BRANCH"
            git stash pop
            git add -A
            if git diff --cached --quiet; then
              echo "Fixes match existing autofix PR #$EXISTING_PR, nothing to add"
              exit 0
            fi
            git commit -m "fix: auto-fix linting issues"
            git push origin "$BRANCH"
            echo "Updated existing PR #$EXISTING_PR"
            exit 0
          fi

          # No PR open, so any leftover branch is stale from a failed run.
          git checkout -b "$BRANCH"
          git add -A
          git commit -m "fix: auto-fix linting issues"
          git push --force origin "$BRANCH"

          gh pr create \
            --base main \
            --head "$BRANCH" \
            --title "fix: auto-fix linting issues" \
            --body "Applies CRLF, Super Linter and shfmt autofixes across the codebase. Opened automatically because main is protected and this workflow can no longer push directly."
