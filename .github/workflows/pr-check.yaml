name: PR Check

on:
  pull_request:
    branches:
      - main

jobs:
  check-addon-changes:
    runs-on: ubuntu-latest
    outputs:
      changedAddons: ${{ steps.find_addons.outputs.changed_addons }}
      changedChangelogFiles: ${{ steps.find_changelog.outputs.changed_changelogs }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Find changed addon directories
        id: find_addons
        run: |
          changed_config_files=$(git diff --name-only "origin/${{ github.base_ref }}" "${{ github.sha }}" | grep -E '^[^/]+/config\.(json|ya?ml)$' || true)
          echo "Changed config files: $changed_config_files"
          changed_addons=$(printf '%s' "$changed_config_files" | awk -F/ '{print $1}' | sort -u | jq -R -s -c 'split("\n") | map(select(length > 0))')
          echo "Changed addons: $changed_addons"
          echo "changed_addons=$changed_addons" >> "$GITHUB_OUTPUT"

      - name: Find changed changelogs
        id: find_changelog
        run: |
          changed_changelogs=$(git diff --name-only "origin/${{ github.base_ref }}" "${{ github.sha }}" | grep -iE '^[^/]+/CHANGELOG\.md$' || true)
          changed_changelogs_json=$(printf '%s' "$changed_changelogs" | jq -R -s -c 'split("\n") | map(select(length > 0))')
          echo "changed_changelogs=$changed_changelogs_json" >> "$GITHUB_OUTPUT"

  check-changelog:
    name: Check CHANGELOG.md updated
    needs: check-addon-changes
    if: ${{ needs.check-addon-changes.outputs.changedAddons != '[]' && needs.check-addon-changes.outputs.changedAddons != '' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        addon: ${{ fromJSON(needs.check-addon-changes.outputs.changedAddons) }}
    steps:
      - name: Check for updated CHANGELOG.md
        run: |
          if ! echo '${{ needs.check-addon-changes.outputs.changedChangelogFiles }}' | jq -e --arg f "${{ matrix.addon }}/CHANGELOG.md" 'index($f) != null' > /dev/null; then
            echo "::error::No new entry in ${{ matrix.addon }}/CHANGELOG.md!"
            exit 1
          fi

  addon-linter:
    name: Addon lint
    needs: check-addon-changes
    if: ${{ needs.check-addon-changes.outputs.changedAddons != '[]' && needs.check-addon-changes.outputs.changedAddons != '' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        addon: ${{ fromJSON(needs.check-addon-changes.outputs.changedAddons) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Run Home Assistant Add-on Lint
        uses: frenck/action-addon-linter@v2
        with:
          path: "./${{ matrix.addon }}"

  check-build:
    name: Test build
    needs: check-addon-changes
    if: ${{ needs.check-addon-changes.outputs.changedAddons != '[]' && needs.check-addon-changes.outputs.changedAddons != '' }}
    runs-on: ${{ matrix.runner }}
    env:
      BUILDX_NO_DEFAULT_ATTESTATIONS: "1"
    strategy:
      fail-fast: false
      matrix:
        addon: ${{ fromJSON(needs.check-addon-changes.outputs.changedAddons) }}
        arch: [amd64, aarch64]
        include:
          - arch: amd64
            runner: ubuntu-24.04
          - arch: aarch64
            runner: ubuntu-24.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Resolve build_from
        id: information
        run: |
          build_file="${{ matrix.addon }}/build.yaml"
          if [[ ! -f "$build_file" ]]; then
            echo "::error::No build.yaml found for ${{ matrix.addon }}"
            exit 1
          fi

          build_from=$(yq -r '.build_from["${{ matrix.arch }}"] // ""' "$build_file")
          if [[ -z "$build_from" || "$build_from" == "null" ]]; then
            echo "::error::No build_from entry for arch '${{ matrix.arch }}' in $build_file"
            exit 1
          fi

          echo "Resolved BUILD_FROM=${build_from}"
          echo "build_from=${build_from}" >> "$GITHUB_OUTPUT"

      - name: Cache docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/buildx-cache
          key: ${{ runner.os }}-buildx-${{ matrix.addon }}-${{ matrix.arch }}-${{ hashFiles(format('{0}/Dockerfile', matrix.addon)) }}
          restore-keys: |
            ${{ runner.os }}-buildx-${{ matrix.addon }}-${{ matrix.arch }}-

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build addon (${{ matrix.arch }})
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.addon }}
          file: ${{ matrix.addon }}/Dockerfile
          push: false
          load: ${{ matrix.addon == 'shelfarr' }}
          provenance: false
          sbom: false
          tags: local/${{ matrix.addon }}-${{ matrix.arch }}:test
          build-args: |
            BUILD_FROM=${{ steps.information.outputs.build_from }}
          cache-from: type=local,src=/tmp/buildx-cache/${{ matrix.arch }}
          cache-to: type=local,dest=/tmp/buildx-cache-new/${{ matrix.arch }}

      - name: Smoke test shelfarr
        if: matrix.addon == 'shelfarr'
        run: |
          set -euo pipefail
          mkdir -p /tmp/smoke/data /tmp/smoke/media /tmp/smoke/share
          echo '{}' > /tmp/smoke/data/options.json

          docker run -d --name shelfarr-smoke \
            -v /tmp/smoke/data:/data \
            -v /tmp/smoke/media:/media \
            -v /tmp/smoke/share:/share \
            -p 18080:80 \
            "local/shelfarr-${{ matrix.arch }}:test"

          healthy=""
          for _ in $(seq 1 40); do
            if curl -fsS "http://localhost:18080/up" > /dev/null 2>&1; then
              healthy="1"
              break
            fi
            sleep 3
          done

          echo "--- container logs ---"
          docker logs shelfarr-smoke || true

          if [[ -z "$healthy" ]]; then
            echo "::error::shelfarr did not become healthy (/up never returned 200)"
            docker rm -f shelfarr-smoke || true
            exit 1
          fi

          echo "--- /up response ---"
          curl -fsS -i "http://localhost:18080/up"

          echo "--- storage symlink check ---"
          docker exec shelfarr-smoke readlink -f /rails/storage
          docker exec shelfarr-smoke readlink -f /audiobooks
          docker exec shelfarr-smoke readlink -f /ebooks
          docker exec shelfarr-smoke readlink -f /downloads

          docker rm -f shelfarr-smoke

      - name: Update cache
        run: |
          rm -rf /tmp/buildx-cache
          mv /tmp/buildx-cache-new /tmp/buildx-cache
